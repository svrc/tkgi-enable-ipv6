---
name:  tkgi-enable-ipv6

releases:
- name:  tkgi-enable-ipv6
  version: 0+dev.1

addons:
  - name:  tkgi-enable-ipv6
    jobs:
    - name: enable-ipv6
      release:  tkgi-enable-ipv6
    include: 
      jobs:
      - name: kubelet
        release: kubo

