---
name:  tkgi-disable-docker-bridge

releases:
- name:  tkgi-disable-docker-bridge
  version: 0+dev.1

addons:
  - name:  tkgi-disable-docker-bridge
    jobs:
    - name: diable-docker-bridge
      release:  tkgi-disable-docker-bridge
    include: 
      jobs:
      - name: kubelet
        release: kubo

